# 🚀 RIOT-X Migration Configuration
# This file contains configuration for the migration pre-flight checker and CloudFormation template
# Copy this to .env and fill in your actual values

# 🔗 Source ElastiCache Configuration
# Option 1: Provide ElastiCache cluster ID directly (recommended for CloudFormation)
ELASTICACHE_CLUSTER_ID=my-elasticache-cluster

# Option 2: Or provide Redis host details (will extract cluster ID if it's an ElastiCache hostname)
REDIS_SOURCE_HOST=my-cluster.abc123.cache.amazonaws.com
REDIS_SOURCE_PORT=6379
REDIS_SOURCE_PASSWORD=
REDIS_SOURCE_TLS=false

# 🔗 Target Redis Cloud Configuration
REDIS_DEST_HOST=redis-15678.c123.eu-west-1-2.ec2.redns.redis-cloud.com
REDIS_DEST_PORT=15678
REDIS_DEST_PASSWORD=your-redis-cloud-password
REDIS_DEST_TLS=true

# 🔧 Additional Configuration
REDIS_SOURCE_DB=0
REDIS_DEST_DB=0
REDIS_TIMEOUT=5
LOG_LEVEL=INFO

# 📍 AWS Configuration (optional - will use AWS CLI default if not specified)
# AWS_REGION=us-east-1
# AWS_PROFILE=default

# 🎯 CloudFormation Parameters (optional overrides)
# ECS_CLUSTER_NAME=riot-migration
# ASSIGN_PUBLIC_IP=ENABLED
# AUTO_CREATE_INTERNET_ACCESS=true
# CONTAINER_CPU=256
# CONTAINER_MEMORY=512
# BATCH_SIZE=50
# THREAD_COUNT=1
# SYNC_MODE=SCAN
# COMPARE_MODE=QUICK
# DRY_RUN=false
